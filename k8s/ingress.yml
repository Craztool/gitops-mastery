apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: gitops-mastery-ingress
spec:
  ingressClassName: nginx
  rules:
    - host: practice.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: gitops-mastery-service
                port:
                  number: 80